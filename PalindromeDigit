class PalindromeDigit {
    public static void main(String[] args) {
        int n=909;
        int o=n;
        int rev=0;
        while(n!=0){
          int u=n%10;
          rev=rev*10+u;
          n=n/10;
        }
        System.out.println(rev==o);
        
    }
}
